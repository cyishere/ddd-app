query GetStartedUnlearnedWords($set_id: Int!) {
  words(
    where: {
      _and: {
        unstarted: { _eq: false }
        learned: { _eq: false }
        set_id: { _eq: $set_id }
      }
    }
  ) {
    id
    set {
      id
      name
      slug
    }
  }
}
